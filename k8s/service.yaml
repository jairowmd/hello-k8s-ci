apiVersion: v1
kind: Service

metadata:
  name: hello-gitops-service

spec:
  selector: # "Procure todos os Pods que tenham essa label."
    app: hello-gitops


  ports:
    - port: 80
      targetPort: 80

  type: NodePort